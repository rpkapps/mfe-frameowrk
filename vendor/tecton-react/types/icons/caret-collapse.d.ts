import type { TectonIconProps } from "./types";
/** CaretCollapseIcon — two chevrons pointing toward each other vertically (down over up) */
export declare function CaretCollapseIcon({ size, strokeWidth, absoluteStrokeWidth, variant, className, ...props }: TectonIconProps): import("react/jsx-runtime").JSX.Element;
export declare namespace CaretCollapseIcon {
    var displayName: string;
}
