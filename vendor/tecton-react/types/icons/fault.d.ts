import type { TectonIconProps } from "./types";
/** FaultIcon — diagonal line with offset crossing segment (geologic fault) */
export declare function FaultIcon({ size, strokeWidth, absoluteStrokeWidth, variant, className, ...props }: TectonIconProps): import("react/jsx-runtime").JSX.Element;
export declare namespace FaultIcon {
    var displayName: string;
}
