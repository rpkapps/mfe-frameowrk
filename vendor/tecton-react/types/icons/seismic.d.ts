import type { TectonIconProps } from "./types";
/** SeismicIcon — horizontal line over concentric arcs (seismic reflection) */
export declare function SeismicIcon({ size, strokeWidth, absoluteStrokeWidth, variant, className, ...props }: TectonIconProps): import("react/jsx-runtime").JSX.Element;
export declare namespace SeismicIcon {
    var displayName: string;
}
