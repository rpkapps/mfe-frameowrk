import type { TectonIconProps } from "./types";
/** ViewColumnIcon — rectangle split into three vertical columns */
export declare function ViewColumnIcon({ size, strokeWidth, absoluteStrokeWidth, variant, className, ...props }: TectonIconProps): import("react/jsx-runtime").JSX.Element;
export declare namespace ViewColumnIcon {
    var displayName: string;
}
