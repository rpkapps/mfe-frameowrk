import type { TectonIconProps } from "./types";
/** StrataIcon — stacked layers with colored (pink-orange gradient) top layer */
export declare function StrataIcon({ size, strokeWidth, absoluteStrokeWidth, variant, className, ...props }: TectonIconProps): import("react/jsx-runtime").JSX.Element;
export declare namespace StrataIcon {
    var displayName: string;
}
