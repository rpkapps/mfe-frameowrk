import type { TectonIconProps } from "./types";
/** CaretUpDownIcon — two chevrons pointing away from each other vertically (up over down) */
export declare function CaretUpDownIcon({ size, strokeWidth, absoluteStrokeWidth, variant, className, ...props }: TectonIconProps): import("react/jsx-runtime").JSX.Element;
export declare namespace CaretUpDownIcon {
    var displayName: string;
}
