import type { TectonIconProps } from "./types";
/** TrajectoryIcon — curve from vertical to horizontal (well trajectory) */
export declare function TrajectoryIcon({ size, strokeWidth, absoluteStrokeWidth, variant, className, ...props }: TectonIconProps): import("react/jsx-runtime").JSX.Element;
export declare namespace TrajectoryIcon {
    var displayName: string;
}
